<table class="table table-bordered">
  <thead>
    <tr>
      <th scope="col">
        <%= link_to admin_coupons_path(sort: "id", direction: sort_direction("id")) do %>
          ID
        <% end %>
      </th>      
      <th scope="col">
        <%= link_to admin_coupons_path(sort: "id", direction: sort_direction("id")) do %>
          Name
        <% end %>
      </th>  
      <th scope="col">
        <%= link_to admin_coupons_path(sort: "id", direction: sort_direction("id")) do %>
          Value
        <% end %>
      </th>       
      <th scope="col">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% coupons.each do |coupon| %>
        <td><%= coupon.id %></td>
        <td><%= coupon.name.capitalize %></td>
        <td><%= coupon.value %></td>
        <td>
          <%= link_to edit_admin_coupon_path(coupon), class: 'btn btn-warning btn-sm' do %>
            <i class="fas fa-edit"></i> Edit
          <% end %>
          <%= link_to admin_coupon_path(coupon), class: 'btn btn-info btn-sm' do %>
            <i class="fas fa-eye"></i> Show
          <% end %>
          <%= link_to admin_coupon_path(coupon), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm', remote: true do %>
            <i class="fas fa-trash-alt"></i> Delete
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
